"""The Orrery deck: a themed terminal face over the engines.

Presentation only. Everything here calls the reconciler and context engines and
renders them; the engines never depend on this layer. Textual is an optional extra.
"""
